services:
  test:
    build:
      dockerfile_inline: FROM alpine
    command: ping localhost
    volumes:
      - /data
    develop:
      watch:
        - path: .
          target: /data
          initial_sync: true
          action: sync+exec
          exec:
            command: echo "SUCCESS"